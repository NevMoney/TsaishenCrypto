// SPDX-License-Identifier: MIT

pragma solidity 0.6.10;

// import "../tokens/TsaishenToken.sol";

// contract TeamTokens is TsaishenToken {
//     TsaishenToken private _tsaishenToken;

//     constructor (contract TsaishenToken token, address beneficiary, uint256 releaseTime) public {
//         token = balanceOf(_tsaishenToken) / 10;
//         beneficiary = address(this);
//         releaseTime = now + 15778463;
//     }
// }